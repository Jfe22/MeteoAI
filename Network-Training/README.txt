Para executar os modelos é recomendado o uso da versão Python 3.12.10 e é necessário criar um ambiente virtual com o seguinte comando:

python -m venv .venv

e de seguida instalar as bibliotecas necessárias com o comando:

pip install -r requirements.txt

É necessário também copiar a pasta "precipitation_dataset" para dentro da pasta "datasets".