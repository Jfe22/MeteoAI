Para executar os scripts data_fetch e dataset_construction é recomendado o uso da versão Python 3.12.10 e é necessário criar um ambiente virtual com o seguinte comando: 

python -m venv .venv

e de seguida instalar as bibliotecas necessárias com o comando:

pip install -r requirements.txt